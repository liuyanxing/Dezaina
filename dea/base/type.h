#pragma once

#include <array>

namespace base {

template <typename T>
using V2 = std::array<T, 2>;

template <typename T>
using V3 = std::array<T, 3>;

template <typename T>
using V4 = std::array<T, 4>;

}