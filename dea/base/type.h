#pragma once

#include <array>

namespace dea::base {

template <typename T>
using v2 = std::array<T, 2>;

template <typename T>
using v3 = std::array<T, 3>;

template <typename T>
using v4 = std::array<T, 4>;

}