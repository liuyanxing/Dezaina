#pragma once

class CommandRegistry {
public:
	CommandRegistry();
	~CommandRegistry();

	void init() {
		// init all actions here
	}

	// return command id
	int registerCommand(Command command) {
		// register command
	}
};